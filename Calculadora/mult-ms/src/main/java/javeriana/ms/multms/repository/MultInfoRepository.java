package javeriana.ms.multms.repository;

import javeriana.ms.multms.model.MultInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MultInfoRepository extends JpaRepository<MultInfo, Long> {
}
